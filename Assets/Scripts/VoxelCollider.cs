using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VoxelCollider : MonoBehaviour
{
    // define a bounds around all geometry
    // check if the bounds is touching a solid voxel position
    // if touching solid voxel, disable movement into that position
    // need to write custom physics to use gravity and elastic collisions instead of using rigidbody???
}
